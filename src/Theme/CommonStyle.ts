import {StyleSheet} from "react-native"

export default StyleSheet.create({
  centerFlex: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center"
  },
  flex: {
    flex: 1
  },
  row: {
    alignItems: "center",
    flexDirection: "row"
  }
})
